version: '3.8'

services:
  jelly-tuner:
    build: .
    env_file:
      - .env
    ports:
      - "5000:5000"
    # --- Hardware Acceleration Support ---
    # Uncomment the section below that matches your hardware.

    # Intel / AMD (VAAPI/QSV)
    # devices:
    #   - /dev/dri:/dev/dri

    # NVIDIA
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
